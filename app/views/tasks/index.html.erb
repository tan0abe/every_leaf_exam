<h2>タスク一覧</h2>
<table>
  <tr>
    <th>タスク名|</th>
    <th>タスク詳細</th>
  </tr>

  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.title %></td>
      <td><%= task.content %></td>
      <td><%= link_to 'タスクの詳細', task_path(task.id) %></td>
      <td><%= link_to 'タスクの編集', edit_task_path(task.id) %></td>
      <td><%= link_to 'タスクの削除', task_path(task.id), method: :delete, data: {confirm: "削除していいですか？" } %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'タスクを作成する', new_task_path %>
